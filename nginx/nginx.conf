events {}

http {
    server {
        listen 8080;

        # 프론트엔드 서버로 프록시
        location / {
            proxy_pass http://localhost:3000;
        }

        # 백엔드 프록시
        location /api {
            proxy_pass http://localhost:8000;
        }
    }
}
